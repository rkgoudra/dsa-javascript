//Print the multiplication table of a given number.

const multiplyBy=2;

const lastNumber=10;

let initiator=1;

do{
console.log(`${multiplyBy}*${initiator} = ${(multiplyBy*initiator)}`);
initiator++
}while(initiator<=lastNumber);